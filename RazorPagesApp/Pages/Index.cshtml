@page
@model IndexModel
@{
    ViewData["Title"] = "Velkommen";
}

<!-- Hero Sektion -->
<div class="container-fluid p-0 mb-5 hero-section-wrapper">
    <div class="row g-0 align-items-stretch">
        <div class="col-12 col-md-6 col-lg-5 order-md-1">
            <div class="hero-text-box" style="background-color: var(--db-primary-red); color: var(--db-white); display: flex; flex-direction: column; justify-content: center; padding: 2rem; ">
                <h1 class="display-4 fw-bold">Lange grisetransporter sætter igen rekord</h1>
                <p class="lead fs-5 mb-4">Aldrig før har danske landmænd sendt så mange grise på lange dyretransporter. I 2024 blev flere end 13 millioner grise sendt ned igennem Europa på ture af mere end otte timers varighed. Den forfærdelige udvikling er en decideret dyrevelfærdskatastrofe.</p>
                <p>
                    <a href="#" class="btn btn-light btn-arrow fw-bold text-uppercase">Det skal forbydes</a>
                </p>
            </div>
        </div>
        <div class="col-12 col-md-6 col-lg-7 order-md-2 hero-image-col" 
             style="background-image: url('https://placehold.co/1000x600/777777/FFFFFF?text=Baggrundsbillede');">
            <!-- CSS klasse 'hero-image-col' vil styre min-height -->
        </div>
    </div>
</div>

<div class="container">
    <!-- Seneste Nyheder -->
    <div class="row mb-5">
        <div class="col-12 text-center mb-4">
            <h2>Seneste nyheder</h2>
            <p class="lead">Hold dig opdateret på dyrevelfærd og vores arbejde.</p>
        </div>
        @if (Model.LatestBlogPosts != null && Model.LatestBlogPosts.Any())
        {
            foreach (var post in Model.LatestBlogPosts)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100" style="background-color: var(--db-white);">
                        @if (!string.IsNullOrEmpty(post.PictureUrl))
                        {
                            <img src="@post.PictureUrl" class="card-img-top" alt="@post.Title" style="height: 200px; object-fit: cover;">
                        }
                        else
                        {
                            <img src="https://placehold.co/600x400/eeeeee/999999?text=Billede+mangler" class="card-img-top" alt="Billede mangler" style="height: 200px; object-fit: cover;">
                        }
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@post.Title</h5>
                            @if(post.IsPublished)
                            {
                                <small class="text-muted mb-2">@post.PublishDate.ToString("dd. MMMM yyyy")</small>
                            }
                            <p class="card-text flex-grow-1">@post.Summary</p>
                            <a asp-page="/Nyheder/Indlaeg" asp-route-id="@post.Id" class="btn btn-outline-danger btn-arrow align-self-start">Læs mere</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <p>Ingen nyheder at vise i øjeblikket.</p>
            </div>
        }
        <div class="col-12 text-center mt-3">
            <a asp-page="/Nyheder/Index" class="btn btn-danger btn-lg btn-arrow">Se alle nyheder</a>
        </div>
    </div>

    <!-- Dyr til Adoption Sektion -->
    <div class="row p-5 mb-5 text-center" style="background-color: var(--db-primary-red); color: var(--db-white);">
        <div class="col-md-8 mx-auto">
            <h2 class="mb-3">Giv et dyr et nyt hjem</h2>
            <p class="lead mb-4">Mange skønne dyr venter på en ny, kærlig familie på vores internater. Se hvem der venter på dig.</p>
            <a asp-page="/Adoption/Index" class="btn btn-light btn-arrow fw-bold">Se dyr til adoption</a>
        </div>
    </div>

    <!-- Bliv Medlem / Støt Sektion -->
    <div class="row text-center mb-5">
        <div class="col-md-6 mb-3">
            <div class="p-5" style="background-color: var(--db-dark-green-accent); color: var(--db-white); height: 100%;">
                <h3 style="color: var(--db-white);">Bliv medlem</h3>
                <p>Som medlem er du med til at sikre vores fortsatte arbejde for dyrene. Dit bidrag gør en forskel!</p>
                <a asp-page="/Medlemskab/Index" class="btn btn-light btn-arrow fw-bold">Læs mere om medlemskab</a>
            </div>
        </div>
        <div class="col-md-6 mb-3">
            <div class="p-5" style="background-color: var(--db-primary-red); color: var(--db-white); height: 100%;">
                <h3 style="color: var(--db-white);">Støt vores sag</h3>
                <p>Din støtte, stor som lille, hjælper os med at redde dyr i nød, oplyse og kæmpe for bedre dyrevelfærd.</p>
                <a asp-page="/StoetOs" class="btn btn-light btn-arrow fw-bold">Giv et bidrag</a>
            </div>
        </div>
    </div>

</div>
